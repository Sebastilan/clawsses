<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:label="SuperBrain"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:keepScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".AdbController"
            android:exported="true">
            <intent-filter>
                <action android:name="com.superbrain.glasses.CONFIG" />
                <action android:name="com.superbrain.glasses.CONNECT" />
                <action android:name="com.superbrain.glasses.DISCONNECT" />
                <action android:name="com.superbrain.glasses.SEND" />
                <action android:name="com.superbrain.glasses.PHOTO" />
                <action android:name="com.superbrain.glasses.LISTEN_START" />
                <action android:name="com.superbrain.glasses.LISTEN_STOP" />
                <action android:name="com.superbrain.glasses.DISPLAY" />
                <action android:name="com.superbrain.glasses.STATUS" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
